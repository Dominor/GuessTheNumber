package org.academiadecodigo.bootcamp36;

public class Game {

    private Player[] players;
    private static final int MAX_NUMBER = 20;
    private static final int NUMBER_PLAYERS = 2;
    private int pick;
    boolean[] rightPicksPerTurn;
    int[] playersPicks;

    /**Game(int[] playersPicks, boolean[] rightPicksPerTurn) {

        this.playersPicks = playersPicks;
        this.rightPicksPerTurn = rightPicksPerTurn;
    }*/

    public static int getMaxNumber() {
        return MAX_NUMBER;
    }

    public void setupPlayers(Player[] players) {
        this.players = players;
    }

    private void checkPicks() {

        for (int i = 0; i < NUMBER_PLAYERS; i++) {
            if (pick == playersPicks[i]) {
                rightPicksPerTurn[i] = true;
            }
        }
    }

    public void run() {

        // Generate own number pick
        this.pick = Randomizer.getRandom(MAX_NUMBER);

        // No player has got it right yet
        /**for (int i = 0; i < NUMBER_PLAYERS; i++) {
         rightPicksPerTurn[i] = false;
         }*/

        // First turn of picks
        for (int i = 0; i < NUMBER_PLAYERS; i++) {
            System.out.println(players[i].getName());
           // playersPicks[i] = players[i].choose();
        }

        /** // Check if any of the 1st turn of picks was right
         for (int i = 0; i < NUMBER_PLAYERS; i++) {
         if (pick == playersPicks[i]) {
         rightPicksPerTurn[i] = true;
         }
         }

         // If there were right picks, then print them out and stop
         for(int i = 0; i < NUMBER_PLAYERS; i++) {
         if (rightPicksPerTurn[i]) {
         System.out.println("Player " + players[i].getName() + " picked right!");
         }
         }*/

        // n - 1 picks left

        /**for (int i = 0; i < NUMBER_PLAYERS; i++) {
            playersPicks[i] = players[i].choose();
        }*/

        /** Check if any of the turn x of picks was right
         for (int i = 0; i < NUMBER_PLAYERS; i++) {
         if (pick == playersPicks[i]) {
         rightPicksPerTurn[i] = true;
         }
         }

         // If there were right picks, then print them out and stop
         for(int i = 0; i < NUMBER_PLAYERS; i++) {
         if (rightPicksPerTurn[i]) {
         System.out.println("Player " + players[i].getName() + " picked right!");
         }
         }

         while(this.pick != player1Pick) {
         System.out.println("Player " + player1.getName() + " picked " + player1Pick + " and guessed wrong.");
         player1Pick = player1.choose();
         }*/

        // System.out.println("Player " + player1.getName() + " guessed right. My pick was " + this.pick);

        for (int i = 0; i < NUMBER_PLAYERS; i++) {
            System.out.println("Player " + players[i]
                    .getName() + " picked " +
                    playersPicks[i]);
        }
    }

}
